import { test, expect } from 'vitest';

test('just works', () => {
  const input = '';
  const result = '';
  const expected = '';
  // expect(result).to...(expected);
});

test.for([
  [],
])('', ([]) => {
  const result = '';
  // expect(result).to...(expected);
});